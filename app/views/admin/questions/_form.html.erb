	<% if @question.errors.any? %>
	<%= render 'shared/errors', resource: @question %>
	<% end %>
	<%= form_with model: [:admin, @test, @question], local: true do |form| %>
	<p>
		<%= form.label :body %>
		<%= form.text_field :body %>
	</p>
	<p><%= form.submit %></p>
	<% end %>
	<%= link_to 'Back', admin_test_questions_path(test_id: @question.test.id ) %>
