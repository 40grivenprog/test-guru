	<tr>
		<th style = 'text-align: left'><%= question.body %></th>
		<th><%= link_to t('.show'),  admin_question_path(question) %></th>
		<th><%= link_to t('.edit'),  edit_admin_question_path(question) %></th>
		<th><%= link_to t('.delete'), admin_question_path(question),
              method: :delete,
              data: { confirm: t('.confirm') } %> </th>
	</tr>
