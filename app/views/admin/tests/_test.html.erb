	<tr>
		<th style='text-align: left'><%= link_to test.title, admin_test_questions_path(test) %></th>
		<th><%= test.level %></th>
		<th><%= test.questions.length %></th>
		<th><%= button_to t('.begin'), start_test_path(test) %></th>
		<th><%= link_to t('.edit'), edit_admin_test_path(test) %></th>
		<th><%= link_to t('.delete'), admin_test_path(test),
              method: :delete,
              data: { confirm: t('.confirm') } %></th>
	</tr>
