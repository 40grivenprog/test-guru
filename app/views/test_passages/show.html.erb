	<h1><%= t('.header', title: @test_passage.test.title) %></h1>
	<h2><%= t('.questions_num', num: @test_passage.test.questions.length) %></h2>
	<h3><%= t('.current_question', current: @test_passage.calculate_question_number) %></h3>
	<b>
		<%= @test_passage.current_question.body %>
	</b>
	<%= form_with(url: test_passage_path(@test_passage), local: true, method: :put) do |form| %>
		<%= form.collection_check_boxes(:answer_ids, @test_passage.current_question.answers, :id, :body, include_hidden: false) do |b| %>
			<p>
				<%= b.check_box(id: "answe_ids#{b.object.id}") %>
				<%= b.label %>
			</p>
		<% end %>
		<p><%= form.submit t('.next') %></p>
	<% end %>
	<%= link_to t('.all'), tests_path %>